{
  "name": "personal-finance",
  "version": "1.0.0",
  "private": true,
  "description": "Personal finance tracker — vanilla HTML/CSS/JS, Supabase backend. No build step.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
